{
    "name": "request-upload",
    "version": "1.0.0",
    "description": "AWS Lambda function in TypeScript to respond upload requests",
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist && rm function.zip",
        "bundle": "npm run clean && npm run build && zip -r function.zip dist"
    },
    "dependencies": {
        "aws-lambda": "^1.0.7"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.129",
        "@types/node": "^20.10.0",
        "typescript": "^5.3.2"
    }
}
