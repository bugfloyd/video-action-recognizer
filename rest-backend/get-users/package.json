{
    "name": "get-users",
    "version": "1.0.0",
    "description": "AWS Lambda function in TypeScript to get the users list",
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist && rm -f function.zip",
        "bundle": "npm run clean && npm run build && zip -r function.zip dist"
    },
    "devDependencies": {
        "@aws-sdk/client-cognito-identity-provider": "^3.465.0",
        "@types/aws-lambda": "^8.10.129",
        "@types/node": "^20.10.0",
        "typescript": "^5.3.2"
    }
}
